# Direnv
.direnv
.pre-commit-config.yaml

# Scala
.bsp/
.idea/
.metals
.bloop
project/project
project/target
target/
**/*.semanticdb

# justfile
.just/notify

# dotenv
.env

# traces and logs
/hydrozoa-trace.jsonl
**/*.log
**/*.mmd

#scratches
.scratch

# Generated shared head config and per-node persistence (produced at runtime)
/head-config.json
/.hydrozoa-data/

# Local scratch / notes (not for commit)
.scratch/

# AI
.claude/
/CLAUDE.local.md

# keygen-fleet output (rosters, private configs, signing keys — never commit)
/config/demo/

# Local template override with real credentials (never commit)
/config/template/peer-private.template.json.local
