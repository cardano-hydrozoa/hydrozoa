<configuration>
    <!-- Console-only config for every subcommand inside the Docker image (serve + the one-shot
         CLIs). The launcher selects it when HYDROZOA_LOGBACK=logback-docker.xml (set in build.sbt's
         Docker/dockerEnvVars); local runs default to the verbose, file-writing logback.xml. No file
         appenders: a container must not write hydrozoa.log / hydrozoa-trace.jsonl into its
         filesystem — logs go to stdout, where `docker logs` / the log aggregator collects them.
         Levels are the quiet container defaults (root warn, hydrozoa info), not the per-actor
         `trace` firehose logback.xml uses for local debugging. -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{48} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Logging must never block a cats-effect compute worker. A synchronous ConsoleAppender does a
         blocking write+flush on the calling thread; if the stdout sink (journald / docker json-file)
         drains slower than the producer, that thread parks in the write and — since consensus logs on
         the compute pool — a stalled worker can freeze block production (the failure mode confirmed in
         the remote ledger). Wrap it in an AsyncAppender with neverBlock=true: the caller offers the
         event to a bounded queue and moves on; on overflow events are dropped, never blocked. The
         default discardingThreshold sheds TRACE/DEBUG/INFO first, so WARN/ERROR survive under load. -->
    <appender name="ASYNC_STDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT"/>
        <neverBlock>true</neverBlock>
        <queueSize>8192</queueSize>
    </appender>
    <root level="warn">
        <appender-ref ref="ASYNC_STDOUT"/>
    </root>
    <logger name="hydrozoa" level="info"/>
    <!-- The RuleBasedActor / CardanoLiaison tracers log under their own top-level logger names (not
         hydrozoa.*), so without these they inherit root=warn and only their error events show. Route
         them at info so the rule-based regime (dispute + evacuation) and the L1 liaison narrate what
         they are doing — `hydrozoa evacuate` runs both. -->
    <logger name="RuleBasedActor" level="info"/>
    <logger name="CardanoLiaison" level="info"/>
    <!-- Crash-recovery narration. These routes log under their own top-level names, so under root=warn
         the boot timeline is invisible: a warm restart that stalls waiting on the remote L2 ledger's
         restore (it rebuilds its whole log first) shows nothing until it errors. Route them at info so
         the connect/restore lifecycle (RemoteL2Ledger: Connecting / Connected) and the actor boot
         (regime managers: Starting / Watching multisig actors) narrate. These fire at boot and on
         regime transitions, not per block, so info stays bounded to the recovery window — the async
         appender also sheds info first under load, so a steady-state burst can't be pinned by these.
         The per-command RemoteL2Ledger Sending / Received stay at debug (below info), so ordinary L2
         traffic does not flood. -->
    <logger name="RemoteL2Ledger" level="info"/>
    <!-- Dialer lifecycle: connect / clean disconnect / failure. Without these the transports log
         failures only, so a coil that is wedged on a half-open socket and one that is merely idle
         produce identical output (see WsDuplex.defaultReadIdleTimeout). -->
    <logger name="CoilPeerWsTransport" level="info"/>
    <logger name="PeerTransport" level="info"/>
    <logger name="HeadMultisigRegimeManager" level="info"/>
    <logger name="CoilMultisigRegimeManager" level="info"/>
    <logger name="com.bloxbean" level="warn"/>
    <logger name="scalus" level="warn"/>
    <logger name="CardanoBackend" level="warn"/>
    <!-- The only record of why a supervised actor failed; never quiet it. -->
    <logger name="Supervision" level="error"/>
</configuration>
